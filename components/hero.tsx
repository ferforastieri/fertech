'use client'
import{motion,useScroll,useTransform}from'framer-motion';import{useRef}from'react';import type{HomeContent,Profile}from'@/lib/content'
export function Hero({profile,home}:{profile:Profile;home:HomeContent}){const ref=useRef<HTMLElement>(null);const{scrollYProgress}=useScroll({target:ref,offset:['start start','end start']});const y=useTransform(scrollYProgress,[0,1],[0,220]);const rotate=useTransform(scrollYProgress,[0,1],[0,18]);return <section className="hero hero-new" ref={ref}><div className="hero-index"><span>PORTFOLIO / 2026</span><span>{home.heroEyebrow}</span></div><motion.div className="hero-type" style={{y}}><p>{profile.role}</p><h1>{home.heroHeadline.split(' ').map((word,i)=><motion.span key={i} initial={{opacity:0,y:100}} animate={{opacity:1,y:0}} transition={{duration:1,delay:.1+i*.055,ease:[.16,1,.3,1]}}>{word}{' '}</motion.span>)}</h1></motion.div><motion.figure style={{rotate}}><img src={profile.photoUrl} alt={profile.name}/><figcaption>{profile.name}<br/>{home.heroDescription}</figcaption></motion.figure><div className="hero-actions"><a href="#work">{home.projectsButtonLabel} ↓</a><a href="/resume/">{home.resumeButtonLabel} ↗</a><a href={profile.contactUrl}>{home.contactButtonLabel} ↗</a></div></section>}
