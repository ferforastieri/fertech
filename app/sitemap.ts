import type{MetadataRoute}from'next';import{getArticles,getProjects}from'@/lib/content';export const dynamic='force-static';export default async function sitemap():Promise<MetadataRoute.Sitemap>{const base=process.env.NEXT_PUBLIC_SITE_URL;if(!base)throw new Error('NEXT_PUBLIC_SITE_URL não configurada.');const[a,p]=await Promise.all([getArticles(),getProjects()]);return[{url:base,priority:1},{url:`${base}/blog/`,priority:.8},{url:`${base}/resume/`,priority:.7},...a.map(x=>({url:`${base}/blog/${x.slug}/`,priority:.7})),...p.map(x=>({url:`${base}/projects/${x.id}/`,priority:.8}))]}
