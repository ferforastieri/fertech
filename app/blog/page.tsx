import type{Metadata}from'next';import Link from'next/link';import{getArticles}from'@/lib/content';export const metadata:Metadata={title:'Artigos',description:'Artigos sobre engenharia de software, produto, arquitetura e carreira.',alternates:{canonical:'/blog/'}};export default async function Blog(){const articles=await getArticles();return <main className="listing shell"><p className="eyebrow">Notas de campo</p><h1>Ideias que merecem<br/><em>ser compartilhadas.</em></h1><div className="article-list">{articles.map(a=><Link key={a.slug} href={`/blog/${a.slug}/`}><small>{a.date} · {a.readTime}</small><h2>{a.title}</h2><p>{a.description}</p><span>↗</span></Link>)}</div></main>}
